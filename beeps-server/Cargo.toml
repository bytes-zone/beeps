[package]
name = "beeps-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.9"
tokio = { version = "1.42.0", features = ["rt-multi-thread", "tracing"] }
tower-http = { version = "0.6.2", features = ["trace"] }

[lints]
workspace = true
